import {ImgMissingDirective} from './img-missing.directive';

describe('ImgMissingDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgMissingDirective();
    expect(directive).toBeTruthy();
  });
});
